---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Newsletter Admin | RelayHub"
  description="Private RelayHub newsletter and early access signup management page."
  noindex={true}
>
  <section class="rh-section">
    <div class="rh-container">
      <p class="rh-kicker">Admin</p>

      <h1 class="rh-section-title">Newsletter / Early Access Signups</h1>

      <p class="rh-section-lead">
        Private admin page for checking early access signups, searching records,
        and exporting CSV data.
      </p>

      <div class="rh-card rh-admin-controls">
        <label class="rh-label" for="admin-token">Admin token</label>
        <input
          class="rh-input"
          id="admin-token"
          type="password"
          placeholder="Paste admin token"
          autocomplete="off"
        />

        <div style="height: var(--space-5);"></div>

        <label class="rh-label" for="search">Search early access signups</label>
        <input
          class="rh-input"
          id="search"
          type="search"
          placeholder="Search name, email, community, or message"
          autocomplete="off"
        />

        <div class="rh-actions">
          <button class="rh-button rh-button-primary" type="button" id="load-signups">
            Load signups
          </button>

          <button class="rh-button rh-button-secondary" type="button" id="search-server">
            Search server
          </button>

          <button class="rh-button rh-button-secondary" type="button" id="export-csv" disabled>
            Export CSV
          </button>

          <button class="rh-button rh-button-secondary" type="button" id="clear-token">
            Clear token
          </button>
        </div>
      </div>

      <div class="rh-card" style="margin-top: var(--space-8);">
        <h2 class="rh-card-title">Summary</h2>
        <p class="rh-card-text" id="summary">No data loaded yet.</p>
      </div>

      <div class="rh-card" style="margin-top: var(--space-8); overflow-x: auto;">
        <h2 class="rh-card-title">Latest signups</h2>

        <table class="rh-admin-table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Created</th>
              <th>Name</th>
              <th>Email</th>
              <th>Community</th>
              <th>Message</th>
            </tr>
          </thead>
          <tbody id="signup-table">
            <tr>
              <td colspan="6">No records loaded.</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <script is:inline>
    (() => {
      const tokenInput = document.querySelector("#admin-token");
      const searchInput = document.querySelector("#search");
      const loadButton = document.querySelector("#load-signups");
      const searchServerButton = document.querySelector("#search-server");
      const exportButton = document.querySelector("#export-csv");
      const clearTokenButton = document.querySelector("#clear-token");
      const summary = document.querySelector("#summary");
      const table = document.querySelector("#signup-table");

      let loadedRows = [];
      let serverTotal = 0;

      const storedToken = sessionStorage.getItem("relayhub_admin_token");
      if (storedToken && tokenInput) {
        tokenInput.value = storedToken;
      }

      function escapeHtml(value) {
        return String(value ?? "")
          .replaceAll("&", "&amp;")
          .replaceAll("<", "&lt;")
          .replaceAll(">", "&gt;")
          .replaceAll('"', "&quot;")
          .replaceAll("'", "&#039;");
      }

      function formatDate(value) {
        if (!value) return "";
        const date = new Date(value);
        if (Number.isNaN(date.getTime())) return String(value);
        return date.toLocaleString();
      }

      function matchesSearch(row, query) {
        if (!query) return true;

        const haystack = [
          row.id,
          row.created_at,
          row.name,
          row.email,
          row.community,
          row.message,
        ]
          .map((value) => String(value ?? "").toLowerCase())
          .join(" ");

        return haystack.includes(query.toLowerCase());
      }

      function normaliseRows(data) {
        if (Array.isArray(data?.signups)) return data.signups;
        if (Array.isArray(data?.rows)) return data.rows;
        return [];
      }

      function renderRows() {
        if (!table || !summary || !exportButton || !searchInput) return;

        const query = searchInput.value.trim();
        const filtered = loadedRows.filter((row) => matchesSearch(row, query));

        const totalText =
          serverTotal && serverTotal !== loadedRows.length
            ? `${serverTotal} total server record(s), ${loadedRows.length} loaded`
            : `${loadedRows.length} signup record(s) loaded`;

        summary.textContent = `${totalText}. Showing ${filtered.length}.`;

        exportButton.disabled = loadedRows.length === 0;

        if (filtered.length === 0) {
          table.innerHTML = `<tr><td colspan="6">No records found.</td></tr>`;
          return;
        }

        table.innerHTML = filtered
          .map((signup) => {
            return `
              <tr>
                <td>${escapeHtml(signup.id)}</td>
                <td>${escapeHtml(formatDate(signup.created_at))}</td>
                <td>${escapeHtml(signup.name)}</td>
                <td>${escapeHtml(signup.email)}</td>
                <td>${escapeHtml(signup.community)}</td>
                <td>${escapeHtml(signup.message)}</td>
              </tr>
            `;
          })
          .join("");
      }

      async function loadSignups({ useServerSearch = false } = {}) {
        if (!tokenInput || !summary || !table) return;

        const token = tokenInput.value.trim();

        if (!token) {
          summary.textContent = "Admin token is required.";
          return;
        }

        sessionStorage.setItem("relayhub_admin_token", token);
        summary.textContent = "Loading early access signups...";

        try {
          const params = new URLSearchParams();

          if (useServerSearch && searchInput?.value.trim()) {
            params.set("q", searchInput.value.trim());
          }

          const query = params.toString();
          const endpoint = query
            ? `/api/admin/newsletter?${query}`
            : "/api/admin/newsletter";

          const response = await fetch(endpoint, {
            headers: {
              Authorization: `Bearer ${token}`,
              Accept: "application/json",
            },
          });

          const data = await response.json().catch(() => null);

          if (!response.ok) {
            throw new Error(data?.error || `Request failed: ${response.status}`);
          }

          loadedRows = normaliseRows(data);
          serverTotal = Number(data?.total || loadedRows.length || 0);

          renderRows();
        } catch (error) {
          loadedRows = [];
          serverTotal = 0;

          table.innerHTML = `<tr><td colspan="6">No records loaded.</td></tr>`;
          summary.textContent =
            error instanceof Error ? error.message : "Failed to load early access signups.";

          if (exportButton) exportButton.disabled = true;
        }
      }

      function toCsv(rows) {
        const headers = [
          "id",
          "created_at",
          "name",
          "email",
          "community",
          "message",
        ];

        const escapeCsv = (value) => {
          const text = String(value ?? "");
          if (/[",\n\r]/.test(text)) {
            return `"${text.replaceAll('"', '""')}"`;
          }
          return text;
        };

        return [
          headers.join(","),
          ...rows.map((row) =>
            headers.map((key) => escapeCsv(row[key])).join(",")
          ),
        ].join("\n");
      }

      function exportCsv() {
        if (loadedRows.length === 0) return;

        const blob = new Blob([toCsv(loadedRows)], {
          type: "text/csv;charset=utf-8",
        });

        const url = URL.createObjectURL(blob);
        const link = document.createElement("a");
        link.href = url;
        link.download = `relayhub-newsletter-${new Date().toISOString().slice(0, 10)}.csv`;
        document.body.appendChild(link);
        link.click();
        link.remove();
        URL.revokeObjectURL(url);
      }

      loadButton?.addEventListener("click", () => loadSignups({ useServerSearch: false }));
      searchServerButton?.addEventListener("click", () => loadSignups({ useServerSearch: true }));
      exportButton?.addEventListener("click", exportCsv);
      searchInput?.addEventListener("input", renderRows);

      clearTokenButton?.addEventListener("click", () => {
        sessionStorage.removeItem("relayhub_admin_token");
        if (tokenInput) tokenInput.value = "";
        if (summary) summary.textContent = "Admin token cleared.";
        loadedRows = [];
        serverTotal = 0;
        if (table) table.innerHTML = `<tr><td colspan="6">No records loaded.</td></tr>`;
        if (exportButton) exportButton.disabled = true;
      });
    })();
  </script>

  <style is:global>
    .rh-admin-controls {
      margin-top: var(--space-8);
    }

    .rh-admin-table {
      width: 100%;
      border-collapse: collapse;
      min-width: 980px;
    }

    .rh-admin-table th,
    .rh-admin-table td {
      border-bottom: 1px solid var(--color-border);
      padding: 0.75rem;
      text-align: left;
      vertical-align: top;
    }

    .rh-admin-table th {
      font-size: 0.78rem;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--color-muted);
      background: var(--color-surface-soft);
    }

    .rh-admin-table td {
      font-size: 0.92rem;
    }

    .rh-admin-table tr:last-child td {
      border-bottom: 0;
    }

    .rh-actions {
      margin-top: var(--space-6);
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-3);
    }
  </style>
</BaseLayout>